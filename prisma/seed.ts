console.log('Seed data is embedded in apps/api/src/store.service.ts for local dev.');
console.log('Prisma schema contains PostgreSQL production models and can be seeded against a real database in deployment.');
