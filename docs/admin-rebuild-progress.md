# 后台重构进度

## Phase 0：现状审计

状态：`COMPLETED`

- 已确认后台仍使用真实 API 与 PostgreSQL。
- 已确认 12 个资源路由复用 `TablePage.vue`，并记录调试控件、常驻详情及长文本铺开问题。
- 已记录情绪脏值和真实表计数。
- 已生成本轮 1366/1440 后台截图基线。

## Phase 1：信息架构和导航

状态：`COMPLETED`

- 已按「总览 / 内容运营 / AI 管理 / 知识与分类 / 系统」重组 13 条真实后台路由；每项仍为正常 RouterLink。
- 已加入可折叠侧边栏、当前页高亮与统一面包屑，并保留既有 `admin-nav-*` 自动化定位标识。
- 已移除正式界面的“选择第一行”“选择最后一行”和全部“查看选中…”测试式选行按钮；选择记录改为点击真实表格行。
- 已将常驻右侧详情栏改为可关闭详情抽屉；树洞图片仍通过真实 MediaAsset URL 显示。
- 为避免破坏既有真实写入 API，操作文本输入暂保留，但已按资源显示具体用途；将在后续页面分拆时替换为资源专用表单。
- 应用内浏览器验证：13/13 导航路由、详情抽屉打开/关闭和侧栏折叠均通过。

## Phase 2：内容运营页面

状态：`COMPLETED`

- 已拆分为 `UsersPage`、`PostsPage`、`RepliesPage`、`FeedbackTicketsPage`，不再由 `TablePage` 承担这四个运营页面。
- 四页都通过真实后台 API 读取列表、统计、筛选和关联信息；详情均由表格行打开可关闭抽屉。
- 用户页面已实际执行“禁言 → 恢复正常 → 恢复禁言”，树洞页面已对已发布记录执行“恢复公开”并由页面重新读取确认。
- 回应审核与反馈工单的可恢复写入回归已完成；PostgreSQL 审计记录已确认。详见 `artifacts/test-report/admin-phase2-content-operations.md`。

## Phase 3：AI 管理页面

状态：`PENDING`

状态更新：`IN_PROGRESS`

- AI 配置中心已拆为按“本地 Ollama / 云模型 / 模板备用”分组的独立卡片页面，连接测试、启停与扫描继续使用真实 API。
- 下一步继续拆分风格路由、AI 任务记录和回复预设，并完成 Ollama 与异步任务验证。
