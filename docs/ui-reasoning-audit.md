# UI 合理性审计

更新时间：2026-08-07

| 页面 | 问题与决策 | 当前结果 |
| --- | --- | --- |
| 广场 | 分类数量变化不能放大容器；保持固定高度横向滚动并隐藏滚动条 | 375/390/430 无横向页面溢出，筛选后卡片稳定 |
| 写心情 | 文本计数、可见范围、回应风格和发布按钮不能被底栏遮挡 | 结构检查通过，真实选择与发布保留 |
| 详情/回复抽屉 | 04 是 03 的 bottom sheet 状态，不新增路由 | 路由和抽屉真实交互通过 |
| 今日回信 | 风格切换只改变选中态和动态内容 | AI 动态生成通过，结构无溢出 |
| 工具 | 八卡等高、真实任务类型，不复用改标题的静态页 | 8 个入口与独立 taskType 通过 |
| 我的 | 三列数字/单位分层、菜单和底栏留白 | 响应式结构检查通过 |
| 我的日记 | 相对媒体 URL 请求到前台端口导致破图 | 统一 `resolveApiUrl` 并补 `/uploads` 代理；夹具真实附件已加载 |
| 后台登录 | 验证码输入缺少稳定可访问名称 | 三项输入补 `aria-label`；静态验证码仍是安全欠账 |
| AI 配置 | 页面显示本地模型但保存逻辑拒绝 Ollama | 删除旧禁令，允许批准的本机地址并真实保存 |

视觉夹具共 96 个响应式检查行：页面横向滚动、控件溢出、控制台错误、乱码均为 0。原始设计像素比较仍有 23 页超过 5%，主要来自动态真实数据、字体/比例和局部素材差异；不使用整页截图或假数据换取分数。

截图：`artifacts/screenshots/front/diary-media-live-2026-08-07.png`、`artifacts/screenshots/front/me-live-2026-08-07.png`、`artifacts/screenshots/admin/ai-providers-live-2026-08-07.png`。完整前后对照在最新 `artifacts/visual-fixtures/v1/runs/`。
