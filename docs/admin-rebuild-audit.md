# 后台管理端重构审计（Phase 0）

审计时间：2026-07-12（Asia/Shanghai）

## 运行基线

| 服务 | 地址/端口 | 结果 |
| --- | --- | --- |
| API | 127.0.0.1:3000 | 运行中 |
| 前台 | 127.0.0.1:5173 | 运行中 |
| 后台 | 127.0.0.1:5174 | 运行中，已确认提供当前 `Layout.vue` 源码 |
| PostgreSQL | 127.0.0.1:55432 | 运行中 |
| Ollama | 127.0.0.1:11434 | 运行中 |

运行时权威数据仍为 PostgreSQL 与现有 API；本阶段未创建 Mock Store、JSON Store 或替代业务接口。

## 代码与路由现状

- 后台路由有登录、总览及 12 个管理资源路由；除 Dashboard 外，资源路由都映射到 `apps/admin/src/views/TablePage.vue`。
- 现有通用页保留真实 API 调用和审计写入，但同时存在不适合正式后台的信息架构：`选择第一行`、`选择最后一行`、常驻右侧详情、通用“备注 / 回复 / 编辑内容”输入框、长文本直接铺开。
- 当前 `Layout.vue` 已开始提供浅色公共外壳与顶部栏，但导航尚未按运营分组，页面级结构尚未拆分。
- AI、反馈、设置等真实写操作已有 API 与 PostgreSQL 持久化证据，不得在重构中替换或删除。

## PostgreSQL 当前数据

| 实体 | 数量 |
| --- | ---: |
| User | 2 |
| Post | 14 |
| Reply | 17 |
| AIJob | 91 |
| FeedbackTicket | 9 |
| AuditLog | 67 |

AIJob 当前均为 `succeeded`；反馈为 3 条 `open`、6 条 `resolved`。

## 数据质量问题

`Post` 与 `Mood` 的情绪字段存在历史脏数据：`??`、`jiaolv`。后续阶段必须迁移/归一为中文合法枚举，且不得丢弃关联的真实业务记录。

## 当前视觉与可用性问题

1. 多个业务资源页共用通用表格/详情模板，页面目标不清。
2. 正式页面仍出现调试选行控件、始终展开的详情栏和通用操作输入框。
3. 表格列和 AI 长文本导致阅读密度过高，缺少抽屉、折叠和明确主操作。
4. 菜单未按总览、内容运营、AI 管理、知识分类、系统分组。
5. 旧后台视觉 diff 仍未达标；本轮已刷新 `artifacts/screenshots/admin/` 下 20 张 1366/1440 基线截图。

## Phase 0 结论

`COMPLETED`。下一阶段只进行信息架构与导航重构：菜单分组、折叠、面包屑、通用布局及移除正式页面调试 UI；不改变现有 API、数据库模型、前台行为或 Ollama 链路。
