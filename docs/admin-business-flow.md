# 管理员业务流

更新时间：2026-08-07

- 登录：真实 API token；账号、密码、验证码输入均有稳定可访问名称。静态验证码仍需后续改为服务端挑战码。
- Dashboard：真实读取用户、待审、反馈和 AI 监控指标，包括 Ollama 在线状态、模型数、调用、成功率、耗时和 fallback。
- 用户：查看详情、禁言/恢复、封禁、备注和标签；状态写入 PostgreSQL，并影响前台权限。
- 内容：查看公开/私密状态、风险、互动和 AI 信息；审核、拒绝、隐藏、恢复、重生成均调用 API。
- 回应：区分 AI/真人回应；支持通过、拦截、编辑后通过，前台同步。
- AI Provider：扫描本机 7 个 Ollama 模型、测试连接、启停、保存配置。
- AI Route：五种风格保存主/备/模板兜底；后台修改后下一次前台 AIJob 记录新路由。
- AI Job：展示任务、输入摘要、模型、状态、输出、耗时、重试和 trace。
- 反馈：先回复再解决，前台可读取状态；分类、FAQ、快捷回复均真实 CRUD。
- 系统设置：只提交变化项并回读；本地模型优先开关真实切换五条路由。
- 审计日志：管理员关键写操作产生 AuditLog/ModerationLog。

当前自动化证据：管理端 17/17 页面与操作检查通过，跨端 4/4，通过独立 PostgreSQL 测试库验证。
