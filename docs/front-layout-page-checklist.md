# 晚安树洞前台 01-14 页面检查清单

生成时间：2026-07-07

| 页面 | 检查结论 | 本轮处理 |
| --- | --- | --- |
| 01 广场 | 可用，横向溢出为 0 | 保留主 tabbar 与写心情悬浮按钮，诊断无文字重叠 |
| 02 写下心情 | 可用，横向溢出为 0 | 保持独立发布流程，无全局 tabbar 干扰 |
| 03 树洞详情 | 可用，横向溢出为 0 | 隐藏全局 tabbar，下移详情回复 dock，压缩回复卡片 |
| 04 回复抽屉态 | 可用，横向溢出为 0 | 保持为 03 内部 bottom sheet 状态，不新增独立路由 |
| 05 今日回信 | 可用，横向溢出为 0 | 修复建议 chips 与底部导航拥挤，压缩卡片和按钮节奏 |
| 06 情绪工具 | 可用，横向溢出为 0 | 压缩工具宫格，重建底部标语，移除重复文字背景 |
| 07 情绪拆解 | 可用，横向溢出为 0 | 独立工具流程不显示全局 tabbar |
| 08 我的 | 可用，横向溢出为 0 | 修复成长卡指标、清空按钮与底部安全区 |
| 09 我的日记 | 可用，横向溢出为 0 | 保留主 tabbar，列表点击流通过 |
| 10 情绪月报 | 可用，横向溢出为 0 | 动作按钮固定在 tabbar 上方，不再被遮挡 |
| 11 我的回信 | 可用，横向溢出为 0 | 重建卡片样式，清除脏裁剪图，按钮可读可点 |
| 12 我的收藏 | 可用，横向溢出为 0 | 保留收藏切换与取消收藏真实交互 |
| 13 隐私设置 | 可用，横向溢出为 0 | 保留开关和数据说明路由交互 |
| 14 帮助与反馈 | 可用，横向溢出为 0 | 压缩 FAQ/表单，上传与提交按钮完整露出 |

## 自动检查摘要

- 文字重叠：`artifacts/layout/front-overlap-report.md` 显示 01-14 在 375/390/414/430 宽度均为 OK。
- 横向滚动：`artifacts/layout/front-horizontal-overflow.md` 显示 01-14 在 375/390/414/430 宽度均为 OK。
- 安全区：`artifacts/layout/front-safe-area.md` 对真实文字和可点击控件进行检查；核心页面均无不可点击遮挡，04 抽屉态保留底部操作区。
- 视觉截图：`artifacts/screenshots/layout-after/` 已生成 01-14 修后截图。
